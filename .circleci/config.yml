version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.8.0
    parallelism: 1
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "1b:79:18:43:5a:c5:48:ea:f0:1e:1c:e5:63:15:9f:3d"
      - run:
          name: setup environment
          command: |
            ssh-keyscan -t ssh-rsa ${SITE_IP}
            ssh-keyscan -t ssh-rsa ${SITE_IP} >> ~/.ssh/known_hosts
      - attach_workspace:
          at: ~/chazmcgrill/developer-portfolio
      - restore_cache:
          key: v1-npm-{{ checksum "package-lock.json" }}
      - run: npm install
      - save_cache:
          key: v1-npm-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run: npm run test
      - run: npm run build
      - run:
          name: deploy to hosting
          command: scp -vvv -r ~/dist ${DEPLOY_USER}@${DEPLOY_SERVER}:${DEPLOY_DIR}/.

version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.8.0
    parallelism: 1
    steps:
      - add_ssh_keys:
          fingerprints:
            - "37:5e:80:3b:6a:15:6e:3c:e2:ab:77:5f:69:d0:c9:35"
      - checkout
      - run:
          name: setup environment
          command: ssh-keyscan $SITE_IP >> ~/.ssh/known_hosts
      - attach_workspace:
          at: ~/chazmcgrill/developer-portfolio
      - restore_cache:
          key: v1-npm-{{ checksum "package-lock.json" }}
      - run: npm install
      - save_cache:
          key: v1-npm-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run: npm run test
      - run: npm run build

      - run:
          name: deploy to hosting
          command: scp -r ~/dist ${DEPLOY_USER}@${DEPLOY_SERVER}:${DEPLOY_DIR}/.  